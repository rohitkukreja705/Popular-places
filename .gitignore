# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
*.lock

# Android platform folder is regenerated by CI on every build — see
# .github/workflows/build.yml — so it is intentionally not committed.
/android/

# IDE
.idea/
*.iml
.vscode/

# macOS
.DS_Store
